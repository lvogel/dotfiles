[init]
        defaultBranch = main
[include]
        path = ~/.gitconfig.local
[push]
        # Automatically setup remote tracking branches
        autoSetupRemote = true
[commit]
        # Sign commits using GPG. Make sure to set a key
        # in ~/.gitconfig.local
        gpgsign = true
[core]
        excludesFile = ~/.gitignore
[log]
        date = iso-local
